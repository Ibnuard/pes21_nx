name: Public tree audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject proprietary and generated files
        shell: pwsh
        run: ./scripts/check-public-tree.ps1
